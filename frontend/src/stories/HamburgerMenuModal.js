import React from "react";
import PropTypes from "prop-types";

export const HamburgerMenuModal = ({ primary }) => {
  return <div>1234</div>;
};

HamburgerMenuModal.propTypes = {
  primary: PropTypes.bool,
};

HamburgerMenuModal.defaultProps = {
  primary: false,
};
