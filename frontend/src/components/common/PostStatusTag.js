import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";

const PostStatusTag = (props) => {
  return <div></div>;
};

PostStatusTag.propTypes = {};

export default PostStatusTag;
